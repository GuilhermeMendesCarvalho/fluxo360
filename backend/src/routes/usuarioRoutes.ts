import { Router } from 'express';
// import { algumController } from '../controllers/usuarioController'; // REMOVA ou comente se ainda não tem

const router = Router();

// router.post('/cadastrar', algumController); // Exemplo, só adicione se tiver mesmo o controller

export default router;
